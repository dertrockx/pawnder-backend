export enum SexEnum {
	Male = "m",
	Female = "f",
}

export enum ActionEnum {
	Adopt = "adopt",
	Foster = "foster",
}

export enum AnimalTypeEnum {
	Dogs = "dogs",
	Cats = "cats",
	FishAndAquariums = "fish and aquariums",
	ReptAndAmphib = "reptiles and amphibians",
	ExoticPets = "exotic pets",
	Rabbits = "rabbits",
	Rodents = "rodents",
}

export enum ApplicationStatusEnum {
	Pending = "pending",
	Deleted = "deleted",
	UnderReview = "under review",
	Rejected = "rejected",
	Accepted = "accepted",
}

export enum PhotoOwnerEnum {
	Pet = "pet",
	User = "user",
	Institution = "institution",
}

export enum PetPhotoTypeEnum {
	Main = "main",
	Other = "other",
}
